define service {
    use                 generic-service
    host_name           vertex-symposium
    service_description App HTTP health
    check_command       check_http_health!30090!/actuator/health
    check_interval      1
    retry_interval      1
}
